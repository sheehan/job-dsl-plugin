buildscript {
    repositories {
        jcenter()
    }
}

apply plugin: 'org.ajoberstar.github-pages'

githubPages {
    repoUri = 'git@github.com:sheehan/job-dsl-plugin.git'
    pages {
        from 'job-dsl-api'
    }
}